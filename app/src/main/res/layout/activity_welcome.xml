<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/welcomebg"
    tools:context=".WelcomeActivity">


    <Button
        android:id="@+id/driver_select_btn"
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="365dp"
        android:layout_marginEnd="75dp"
        android:background="@android:color/background_light"
        android:text="I'm a Driver"
        android:textAllCaps="true"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="20sp" />

    <Button
        android:id="@+id/customer_select_btn"
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:layout_below="@id/driver_select_btn"
        android:layout_alignStart="@+id/driver_select_btn"
        android:layout_marginStart="0dp"
        android:layout_marginTop="31dp"
        android:background="@android:color/holo_red_dark"
        android:text="I'm a Customer"
        android:textAllCaps="true"
        android:textColor="@android:color/background_light"
        android:textSize="20sp" />

</RelativeLayout>